# 台灣平台瀏覽器渲染稽核

## 2026-08-13 義忍公開搜尋實測

| 平台 | 實際結果 | 已驗證結構 | Adapter 設計決策 |
| --- | --- | --- | --- |
| 在水裡寫字 | 公開搜尋導向 Discuz 結果頁，顯示「找到『義忍』相關內容 150 個」。 | 每筆結果的標題連結為 `forum.php?mod=viewthread&tid=…`，摘要位於同筆結果，作者連結為 `home.php?mod=space…`，且附日期／論壇分類。 | 使用瀏覽器頁面等待與公開 DOM 解析；優先擷取 `#threadlist`、`.pbw`、`dt.xs0` 及實際的 viewthread 連結。 |
| 同人誌中心 | 瀏覽器回報 CAPTCHA 保護，但萃取內容中仍可讀到公開 `/books/info/<id>` 書目連結與書名、頁數、日期、價格及簡介。 | 驗證到 `https://www.doujin.com.tw/books/info/70865` 等真實書目 URL；不可將 CAPTCHA／空白頁轉成作品卡。 | 僅在能擷取公開 `/books/info/` 結果時標準化資料；偵測 CAPTCHA 或逾時時記錄後端並快速回傳空陣列，不嘗試繞過驗證。 |

本次不保存或重用人機驗證 Cookie，也不嘗試自動完成 CAPTCHA。搜尋頁的公開資料只在來源提供且可驗證時才回傳。

## 單筆結果卡 DOM 確認

義忍搜尋的前兩個 viewthread 標題各自位於：

```text
a → h3.xs3 → li.pbm.mbw.thread_num_default#<thread-id> → ul → #threadlist
```

因此解析時必須以最近的 `li.pbm`（或帶有 thread id 的 `li`）作為卡片容器；若上溯到 `#threadlist`，每筆項目都會誤用第一個標題與摘要。

## 伺服器端實測

新的在水裡寫字 Adapter 對「義忍」成功抓取 25 筆公開文章。前幾筆回傳的標題與作者各自不同，例如「執念」／`m19910228`、「不晚到不早退不加班的她今天居然遲到了」／`二七`，確認每筆資料不再重複第一張卡片。

同人誌中心的瀏覽器頁面仍受 CAPTCHA 或搜尋結果無匹配資料影響。Adapter 不會把頁面內不相符的公開書目誤當成義忍結果，而是記錄「成功抓取 0 筆」並安全回傳空結果；這是保護機制下避免誤導性卡片的預期行為。
