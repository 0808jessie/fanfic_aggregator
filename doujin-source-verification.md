# 同人誌中心來源驗證紀錄

驗證時間：2026-08-13（GMT+8）

## 公開搜尋頁

指定來源為 <https://www.doujin.com.tw/books/search?q=%E7%BE%A9%E5%BF%8D>。瀏覽器文字擷取曾顯示 `/books/info/{id}` 詳細頁連結、封面圖片、作品名、頁數、價格與簡介等公開卡片 metadata；但從專案伺服器發出的唯讀 HTTP 請求取得 Cloudflare 的 `HTTP 403`／`Just a moment...` 驗證頁。

因此同人誌中心 Adapter 採取下列安全策略：只接受 `https://www.doujin.com.tw/books/info/` 連結、僅保留可見 metadata 內含查詢字的卡片、遇到驗證挑戰時不繞過保護且回傳空結果與 `[同人誌中心] Request blocked` 警示。此警示不會中斷 AO3 等平行 Adapter。

## 混合搜尋驗證

以 AO3 與同人誌中心平行搜尋「義忍」時，AO3 回傳 37 筆即時、可驗證的 AO3 結果；同人誌中心回傳 `HTTP 403` 隔離警示。前端如實顯示警示，未產生任何虛構或未驗證的同人誌中心卡片。

## 個人化流程預覽驗證

已在專案預覽中完成「義忍」搜尋、套用預設 CP 對照、收藏 AO3 作品、填寫五星評分／筆記／三個自訂標籤，並切換至「我的閱讀清單」確認資料顯示。亦已開啟 CP 詞庫，新增、編輯後刪除一筆驗證用對照，預設詞庫保持不變。最後以「長篇／僅看已完結／最新更新」設定按下「設為預設篩選」，頁面顯示保存確認訊息。

重新載入預覽頁後，「我的閱讀清單 1」與「最近搜尋：義忍」仍存在；重新開啟篩選面板後，字數、完結與排序分別恢復為「10,000 字以上／僅看已完結／最新更新」。
