# 指定 Reader 來源觀察紀錄

- 在水裡寫字 `thread/86226` 的公開頁標題為「[鬼滅之刃│義忍] 如願，如常 [PG]」。以一般公開瀏覽開啟時，頁面僅顯示登入框與極少量可抽取文字，顯示伺服端輸出可能未含完整樓層資料；後續實作僅可使用同一公開回應所含的 post/author 資料，並保留來源保護時的安全降級。
- Penana `story/198592` 的公開故事頁提供單一章節目錄，章節錨點為 `issue_1969479`。故事首頁本身未含正文，Reader 必須先正規化至 issue 網址，再從最內層正文容器抽取一次。
- 不對受保護來源使用 Cookie 擷取、登入、驗證繞過或隱藏 WebView；若即時回應未提供公開樓層／正文，維持可辨識的來源級降級。
